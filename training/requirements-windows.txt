# Simplified requirements for Windows
# This file provides a Windows-compatible installation
#
# ⚠️ CRITICAL: TensorFlow requires Python 3.8-3.11 ONLY
# Python 3.12, 3.13, 3.14, etc. are NOT supported!
#
# If you get "Could not find a version that satisfies the requirement tensorflow":
# 1. You're probably using Python 3.12+ (check with: python --version)
# 2. Install Python 3.11 from: https://www.python.org/downloads/release/python-3119/
# 3. Use Python 3.11: py -3.11 -m venv venv
# 4. See INSTALL_PYTHON311.md for step-by-step instructions
#
# Check your version: python --version
# It MUST show Python 3.8, 3.9, 3.10, or 3.11 (NOT 3.12+!)

# Core Deep Learning
# TensorFlow 2.15+ supports Python 3.8-3.11 on Windows
# For Python 3.11, use: pip install tensorflow
# For older Python, you may need: pip install tensorflow==2.13.0
tensorflow>=2.13.0

# TensorFlow.js Conversion (optional - only needed for model conversion)
# tensorflowjs>=4.11.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-learn>=1.3.0

# Dataset Download (optional - only needed for training)
# gdown>=4.7.0

# Visualization (optional - only needed for plotting)
# matplotlib>=3.7.0
# seaborn>=0.12.0

# Jupyter (optional - only needed for notebooks)
# jupyter>=1.0.0
# ipykernel>=6.25.0
# ipywidgets>=8.1.0

# Utilities
tqdm>=4.66.0

# API Server (required for inference API)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Note: Install with: pip install -r requirements-windows.txt
# This is optimized for Windows and includes only essential packages for inference

